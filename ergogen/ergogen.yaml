units:
  # Proxy Spacing Variables
  kx: cx
  ky: U
  # Padding Variables
  px: kx + 2
  py: ky + 2
points:
  zones:
    matrix:
      columns:
        pinky:
          rows:
            mod:
              skip: true
        ring:
          rows:
            mod:
              skip: true
        middle:
        index:
        inner:
      rows:
        mod:
        bottom:
        home:
        top: